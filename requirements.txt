# Dependencias de la app. No incluye rasterio ni geopandas a proposito: la app
# solo lee parquet y un geojson ya simplificado, y arrastrar GDAL al hosting
# alargaria el despliegue sin dar nada.
streamlit>=1.40
pandas>=2.2
pyarrow>=16
plotly>=5.24
numpy>=1.26
